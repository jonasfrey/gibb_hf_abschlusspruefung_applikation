# .NET
backend/**/bin/
backend/**/obj/
backend/**/*.user
backend/**/.vs/
backend/**/appsettings.Development.json

# Node / Vue
frontend/node_modules/
frontend/dist/
frontend/.env.local

# Docker / Secrets (nur Root-.env mit DB-Passwoertern; frontend/.env ist harmlos)
/.env

# TLS-Zertifikate (lokal mit mkcert erzeugt, nicht ins Repo)
deploy/certs/*.pem

# Abgabe-ZIPs
*.zip

# OS
.DS_Store
Thumbs.db
