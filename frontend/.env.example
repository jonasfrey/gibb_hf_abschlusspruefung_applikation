# Basis-URL der PROG2 REST-API.
# Lokal (Backend via dotnet run):  http://localhost:5000
# Im Docker-Stack (ueber Reverse Proxy, relativ):  /api  -> dann leer lassen und
# api/client.js nutzt same-origin. Siehe DEP-Dokumentation.
VITE_API_BASE=http://localhost:5000
