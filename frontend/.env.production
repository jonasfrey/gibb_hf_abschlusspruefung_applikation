# Produktions-Build (Docker-Stack): leere Basis-URL = same-origin.
# Aufrufe gehen an /api/... und werden vom Reverse Proxy (Caddy) ans Backend
# geroutet. Diese Datei hat im Build Vorrang vor .env.
VITE_API_BASE=
