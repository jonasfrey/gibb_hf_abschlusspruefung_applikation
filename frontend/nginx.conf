server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: unbekannte Pfade (Vue Router History-Mode) auf index.html leiten.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
